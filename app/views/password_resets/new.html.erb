<div class="row">
  <div class="reset-maile-send">
    <h1>パスワード再設定</h1>
    <%= form_for(:password_reset, url: password_resets_path) do |f| %>
      <div class="form-group">
        <%= f.label :email, 'メールアドレス' %>
        <small>登録されたメールアドレスをご入力ください</small>
        <%= f.email_field :email, class:'form-control' %>
      </div>
      <%= f.submit 'パスワード再設定メール送信', class: 'maile-btn' %>
    <% end %>
  </div>
</div>